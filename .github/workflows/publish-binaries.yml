name: Windows installer build bundle

on:
  push:
    tags:
    - 'v*'

jobs:
    compile-installer:
        name: Build windows installer exe
        runs-on: ubuntu-latest

        steps:
          - name: Check out the repo
            uses: actions/checkout@v2
          - name: Set up QEMU
            uses: docker/setup-qemu-action@v1
          - name: Set up Docker Buildx
            uses: docker/setup-buildx-action@v1
          - name: Login to DockerHub
            uses: docker/login-action@v1
            with:
              username: ${{ secrets.DOCKERHUB_USERNAME }}
              password: ${{ secrets.DOCKERHUB_TOKEN }}
          - name: Build and push
            uses: docker/build-push-action@v2
            with:
              context: .
              push: true
              tags: rancorsoft1/ezdmb:latest
          - name: Run the installer build process with Docker
            uses: addnab/docker-run-action@v3
            with:
                image: rancorsoft1/ezdmb:latest
                options: -v ${{ github.workspace }}:/work --user root
                run: |
                    iscc ezdmb-installer.iss
          - uses: actions/upload-artifact@v4
            with:
              name: ezdmb_setup.exe
              path: ezdmb_setup.exe
              retention-days: 1

    bundle-install-scripts:
        # This job bundles the installer exe with install.bat and child.ps1
        needs: compile-installer
        name: Bundle installer with necessary platform scripts
        runs-on: ubuntu-latest
        permissions: write-all

        steps:
          - name: Check out the repo
            uses: actions/checkout@v2
          - uses: actions/download-artifact@v4
            with:
              name: ezdmb_setup.exe
          - name: Move installer exe to build artifacts directory
            run:  mv ezdmb_setup.exe artifacts/
          - name: Bundle installer with scripts
            run: |
              mkdir -p installbundle
              mkdir -p installbundle/ezdmb_installer_dependencies
              ls -la
              cp windows/windows_install/install.bat installbundle/
              cp artifacts/ezdmb_setup.exe installbundle/ezdmb_installer_dependencies/
              cp windows/windows_install/child.ps1 installbundle/ezdmb_installer_dependencies/
              cd installbundle
              zip -r ezdmb_install.zip ./*
          - name: Create Release
            id: create_release
            uses: actions/create-release@v1
            env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            with:
              tag_name: ${{ github.ref }}
              release_name: Release ${{ github.ref }}
              draft: false
              prerelease: false
          - name: Upload Release Asset
            id: upload-release-asset 
            uses: actions/upload-release-asset@v1
            env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            with:
              # This pulls from the CREATE RELEASE step above, referencing it's ID to get its 
              # outputs object, which include a `upload_url`. See this blog post for more info:
              # https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
              upload_url: ${{ steps.create_release.outputs.upload_url }}
              asset_path: installbundle/ezdmb_install.zip
              asset_name: ezdmb_install.zip
              asset_content_type: application/zip
